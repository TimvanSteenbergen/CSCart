{if $addons.janrain.appdomain}
<div class="janrain-wrap">
    <span class="janrain-wrap__or">{__("or")}</span>{include file="buttons/button.tpl" but_role="text" but_meta="janrainEngage nobg" but_meta="ty-btn__secondary" but_text=__("janrain_social_login")}
</div>
{/if}

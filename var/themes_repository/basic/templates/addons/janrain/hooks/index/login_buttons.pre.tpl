{if $addons.janrain.appdomain}
<div class="janrain-wrap">
    <span>{__("or")}</span>{include file="buttons/button.tpl" but_role="text" but_meta="janrainEngage nobg" but_text=__("janrain_social_login")}
</div>
{/if}
